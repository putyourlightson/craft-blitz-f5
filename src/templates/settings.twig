{% import '_includes/forms' as forms %}

{{ forms.autosuggestField({
    label: 'Base URL'|t('blitz-f5'),
    instructions: 'The base URL of the API endpoint.'|t('blitz-f5'),
    placeholder: 'https://acmecorp.console.ves.io',
    suggestEnvVars: true,
    suggestions: craft.cp.getEnvSuggestions(),
    name: 'baseUrl',
    value: purger.baseUrl,
    errors: purger.getErrors('baseUrl'),
    required: true,
}) }}

{{ forms.autosuggestField({
    label: 'Namespace'|t('blitz-f5'),
    instructions: 'The CDN namespace.'|t('blitz-f5'),
    placeholder: 'namespace',
    suggestEnvVars: true,
    suggestions: craft.cp.getEnvSuggestions(),
    name: 'namespace',
    value: purger.namespace,
    errors: purger.getErrors('namespace'),
    required: true,
}) }}

{{ forms.autosuggestField({
    label: 'Name'|t('blitz-f5'),
    instructions: 'The CDN distribution name.'|t('blitz-f5'),
    suggestEnvVars: true,
    suggestions: craft.cp.getEnvSuggestions(),
    name: 'name',
    value: purger.name,
    errors: purger.getErrors('name'),
    required: true,
}) }}

{{ forms.autosuggestField({
    label: 'API Token'|t('blitz-f5'),
    instructions: 'The API token.'|t('blitz-f5'),
    placeholder: '',
    suggestEnvVars: true,
    suggestions: craft.cp.getEnvSuggestions(),
    name: 'apiToken',
    value: purger.apiToken,
    errors: purger.getErrors('apiToken'),
    required: true,
}) }}

{{ forms.lightswitchField({
    label: 'Hard Purge'|t('blitz-f5'),
    instructions: 'Whether to remove the content from the distribution, forcing the next request to retrieve the content from the origin server. With this off, the content will be replaced on the next request if the content is stale.'|t('blitz-f5'),
    name: 'hardPurge',
    on: purger.hardPurge,
    errors: purger.getErrors('hardPurge'),
}) }}
